var scene_tree_crawler = (function() {
    var tree;
    
    function set_background(src) {
        
    }

    return {
        load_tree: function(t) {
            tree = t;
        },
    };
})();

//alias
var STC = scene_tree_crawler;